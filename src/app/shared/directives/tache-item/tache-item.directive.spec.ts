import { TacheItemDirective } from './tache-item.directive';

describe('TacheItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TacheItemDirective();
    expect(directive).toBeTruthy();
  });
});
